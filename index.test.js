import {
} from "./index.js";
import { assert, expect, should } from 'chai';


describe("generateStartMatrix", function() {
    it("создает матрицу", function() {
        expect(matrix).to.be.an('array');
    });
});

